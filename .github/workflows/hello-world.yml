name: Hello-world
on: 
   push:
        branches:
                 - master

jobs:
  firs-hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: Print message
        run: echo "Hello wordls tested"

    
