name: hello-world
on:
   push:
       branches:
                 - master
jobs: 
 first-hello-world-job:
    runs-on: ubuntu-latest
    steps: 
            - name: checkout code
              uses: actions/checkout@v3
            - name: print message
              run: echo "hello world ga testing"
                
