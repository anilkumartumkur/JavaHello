name: Just Printing Hello Message
on:
 push:
  branches: [main]
jobs:
 hello-world:
  runs-on: ubuntu:latest
  steps:
    - name: checkout
      uses: actions/checkout@v3
    - name: print
      run: echo "hello World"
